# Screen: Discovery Hub (ADD, P1)

Route: /projects/:id/discovery. Contract: discovery.md. CURRENT.
Explains itself: what discovery is for, when it helps, what it prefills downstream ("Discovery findings can prefill your brief's objectives and methods"). Optional — never a gate; "Skip to brief" always visible.
Three type cards (desk research / stakeholder synthesis / survey), each stating inputs + what it emits. Forms per modal-field-contracts (topic, description, files, question focus; survey adds survey name). Drag-drop upload. Survey card routes into the survey pipeline (own spec).
Artifacts list below with per-artifact "used by brief(s)" forward links (F10 lineage hop).