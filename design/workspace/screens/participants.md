# Screen: Participants & Sessions (ADD, P2)

Basic ops only — NOT a recruiting CRM. Roster: PT-code first (identity separation visible: name column only in fieldwork context, never in evidence-oriented views), scheduled date/time, location/method, notes, session status, accommodations. Add participant: code DERIVED (next PT-NNN preview), all fields optional per runtime. Observer ASSIGNMENTS are CURRENT (participant-tracker runtime): participant drawer carries a SESSION OBSERVERS section — team observers auto-approved ("✓ observing"), stakeholder requests arrive pending with Approve/Decline (researcher decides). Observers who attend can submit session notes into the normal DB quarantine (manual-notes approve/reject). The observer ROLE (Workspace login, read-only access) stays DEFERRED per Phase B ruling — gap report. Class: table + side drawer detail.